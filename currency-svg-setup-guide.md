# دليل إعداد رموز العملات SVG - التحديث الأخير

## ✅ التعديل الجديد: ترتيب العرض

**تم تعديل ترتيب العرض ليكون: السعر أولاً ثم رمز SVG**

### مثال على التنسيق الجديد:

```
149🔸 (بدلاً من 🔸149)
170💎 (بدلاً من 💎170)
```

## كيفية الوصول لإعدادات العملة

1. **تسجيل الدخول إلى لوحة الإدارة**
2. **الذهاب إلى Tools > Currency Settings**
    - أو استخدم الرابط: `/admin/currency`

## كيفية إعداد رموز SVG للعملات

### 1. إعداد رمز SVG للعملة الحالية

-   في قسم "SVG Symbol Settings"
-   أدخل كود SVG في الحقل المخصص
-   اضغط "Update SVG Symbol"

### 2. إعداد رموز SVG لعملات محددة

-   انتقل إلى قسم "Currency-Specific SVG Settings"
-   ستجد جميع العملات النشطة مع حقول منفصلة لكل منها
-   أدخل كود SVG للعملة المطلوبة
-   اضغط "Update" للعملة المحددة

## كيفية عمل النظام

### عرض السعر والرمز منفصلين

```php
// السعر فقط بدون رمز العملة
{{ $tour->display_price_only }}

// رمز العملة SVG منفصل
{!! $tour->currency_svg !!}
```

### النظام الديناميكي للرموز

1. **أولوية البحث:**

    - رمز SVG خاص بالعملة المحددة
    - رمز SVG العام للعملة الحالية
    - رمز نصي (fallback)

2. **التبديل التلقائي:**
    - عند تغيير العملة من القائمة العلوية
    - يتم عرض الرمز الصحيح تلقائياً

## أمثلة لرموز SVG

### رمز الدولار الأمريكي

```svg
<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17h2v-2h-2v2zm0-4h2V9h-2v6z"/>
</svg>
```

### رمز اليورو

```svg
<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
  <path d="M11 2v20c-5.07-.5-9-4.79-9-10s3.93-9.5 9-10zm2.03 0v20c5.07-.5 9-4.79 9-10s-3.93-9.5-9-10z"/>
</svg>
```

## الملفات المُعدّلة

### الملفات الأساسية

-   `app/Helpers/AppHelper.php` - دوال المساعدة الجديدة
-   `modules/Booking/Models/Bookable.php` - خصائص السعر والرمز المنفصلة
-   `modules/Core/Admin/CurrencyController.php` - تحكم إدارة العملات

### ملفات العرض

-   `modules/Tour/Views/frontend/layouts/details/tour-form-book.blade.php`
-   `modules/Tour/Views/frontend/loop-grid.blade.php`
-   `modules/Core/Views/admin/currency/index.blade.php`

## ميزات النظام

✅ **عرض السعر والرمز منفصلين**
✅ **رموز SVG ديناميكية حسب العملة**
✅ **واجهة إدارة سهلة للرموز**
✅ **نظام fallback آمن**
✅ **متوافق مع نظام تبديل العملات**

## حل المشاكل

### إذا لم يظهر الرمز SVG

1. تأكد من صحة كود SVG
2. تحقق من إعدادات العملة في لوحة الإدارة
3. امسح cache التطبيق: `php artisan cache:clear`

### إذا ظهر رمز خاطئ

1. تحقق من أن العملة محددة بشكل صحيح في session
2. تأكد من وجود رمز SVG للعملة المحددة
3. استخدم أدوات التطوير لفحص العملة الحالية

---

**ملاحظة:** هذا النظام يدعم جميع العملات المُفعّلة في التطبيق ويعمل تلقائياً مع نظام تبديل العملات الموجود.
